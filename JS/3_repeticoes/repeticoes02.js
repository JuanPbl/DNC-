const bolsa = [
    "notebook",
    "moletom",
    "celular",
    "carregador",
    "mouse"
]

for (let i=0;i<bolsa.length;i++){
    console.log(bolsa[i])
    if(bolsa[i] == 'mouse') break
}

